# triops runtime state — never commit
app/config.php
app/data/*
!app/data/.htaccess
!app/data/index.php

# build output — releases are attached to GitHub Releases, not committed
dist/

# editors / os
*~
*.log
.DS_Store

# node (dev tooling only, never at runtime)
node_modules
coverage
.nyc_output
*.tgz

# staged from app/assets by dev/build-site.sh
pages/assets/bitwrench.umd.min.js
pages/assets/triops.js
pages/assets/triops-logo.png
pages/assets/favicon.ico

# dev tooling (playwright); never ships
dev/node_modules
dev/package-lock.json

pages/assets/version.js
